package tss.main.object.track;

public class Bahrain {
    enum part {
        i//nt t10s = 63;
        //int t05s = 31;
        //int t01s = 6;
        //straight(575, t10s*6);
        //turn1entry(148, t10s*4 + t05s); //10.5
        //turn2entry(87, t10s*2 + t05s*1 + t01s); // 13.1 723
        //turn3exit(267, t10s*2 + t05s + t01s*4); // 15.9 990
        //drs1(410,t10s*5 + t05s); // 21.4 1400
        //turn4entry(100, t10s*2 + t05s); //24.1 1500
        //turn4exit(300, t10s*4 + t05s); //28.6 1800 s1 end
        //turn5entry(/*distance*/, /*tick*/);
        //turn5exit(/*distance*/, /*tick*/);
        //turn6entry(/*distance*/, /*tick*/);
        //turn6exit(/*distance*/, /*tick*/);
        //turn7entry(/*distance*/, /*tick*/);
        //turn7exit(/*distance*/, /*tick*/);
        //turn8entry(/*distance*/, /*tick*/);
        //turn8exit(/*distance*/, /*tick*/);
        //straight2(/*distance*/, /*tick*/);
        //turn9entry(/*distance*/, /*tick*/);
        //turn9exit(/*distance*/, /*tick*/);
        //turn10entry(/*distance*/, /*tick*/);
        //turn10exit(/*distance*/, /*tick*/);
        //straight3(/*distance*/, /*tick*/);
        //turn11entry(/*distance*/, /*tick*/);
        //turn11exit(/*distance*/, /*tick*/);
        //turn12entry(/*distance*/, /*tick*/);
        //turn12exit(/*distance*/, /*tick*/);
        //turn13entry(/*distance*/, /*tick*/);
        //turn13exit(/*distance*/, /*tick*/);
        //straight4(/*distance*/, /*tick*/);
        //turn14entry(/*distance*/, /*tick*/);
        //turn14exit(/*distance*/, /*tick*/);
        //straight5(/*distance*/, /*tick*/);
        //pitwall(/*distance*/, /*tick*/);
        //private final int distance; // 거리 (미터)
        //private final int tick;     // 소요 시간 (틱 단위)

        //// 3. 생성자 (위에서 괄호 안에 넣은 숫자를 여기서 받습니다)
        //part(int distance, int tick) {
        //    this.distance = distance;
        //    this.tick = tick;
        //}

        //// 4. 데이터를 꺼내 쓸 수 있는 메소드 (Getter)
        //public int getDistance() {
        //    return distance;
        //}

        //public int getTick() {
        //    return tick;
        //}
        // total tick 5,574
        // s1 1,812
        // s2 2469
        // s3 1,293
    }
}
