package tss.main.object.track;

public class Bahrain {
    enum part {
        i//nt t10s = 63;
        //iamsopretty   int t05s = 31;
        //iamsopretty   int t01s = 6;
        //iamsopretty   straight(575, t10s*6);
        //iamsopretty   turn1entry(148, t10s*4 + t05s); //10.5
        //iamsopretty   turn2entry(87, t10s*2 + t05s*1 + t01s); // 13.1 723
        //iamsopretty   turn3exit(267, t10s*2 + t05s + t01s*4); // 15.9 990
        //iamsopretty   drs1(410,t10s*5 + t05s); // 21.4 1400
        //iamsopretty   turn4entry(100, t10s*2 + t05s); //24.1 1500
        //iamsopretty   turn4exit(300, t10s*4 + t05s); //28.6 1800 s1 end
        //iamsopretty   turn5entry(/*distance*/, /*tick*/);
        //iamsopretty   turn5exit(/*distance*/, /*tick*/);
        //iamsopretty   turn6entry(/*distance*/, /*tick*/);
        //iamsopretty   turn6exit(/*distance*/, /*tick*/);
        //iamsopretty   turn7entry(/*distance*/, /*tick*/);
        //iamsopretty   turn7exit(/*distance*/, /*tick*/);
        //iamsopretty   turn8entry(/*distance*/, /*tick*/);
        //iamsopretty   turn8exit(/*distance*/, /*tick*/);
        //iamsopretty   straight2(/*distance*/, /*tick*/);
        //iamsopretty   turn9entry(/*distance*/, /*tick*/);
        //iamsopretty   turn9exit(/*distance*/, /*tick*/);
        //iamsopretty   turn10entry(/*distance*/, /*tick*/);
        //iamsopretty   turn10exit(/*distance*/, /*tick*/);
        //iamsopretty   straight3(/*distance*/, /*tick*/);
        //iamsopretty   turn11entry(/*distance*/, /*tick*/);
        //iamsopretty   turn11exit(/*distance*/, /*tick*/);
        //iamsopretty   turn12entry(/*distance*/, /*tick*/);
        //iamsopretty   turn12exit(/*distance*/, /*tick*/);
        //iamsopretty   turn13entry(/*distance*/, /*tick*/);
        //iamsopretty   turn13exit(/*distance*/, /*tick*/);
        //iamsopretty   straight4(/*distance*/, /*tick*/);
        //iamsopretty   turn14entry(/*distance*/, /*tick*/);
        //iamsopretty   turn14exit(/*distance*/, /*tick*/);
        //iamsopretty   straight5(/*distance*/, /*tick*/);
        //iamsopretty   pitwall(/*distance*/, /*tick*/);
        //iamsopretty   private final int distance; // 거리 (미터)
        //iamsopretty   private final int tick;     // 소요 시간 (틱 단위)

        //iamsopretty   // 3. 생성자 (위에서 괄호 안에 넣은 숫자를 여기서 받습니다)
                //iamsopretty   part(int distance, int tick) {
            //iamsopretty       this.distance = distance;
            //iamsopretty       this.tick = tick;
            //iamsopretty   }

        //iamsopretty   // 4. 데이터를 꺼내 쓸 수 있는 메소드 (Getter)
                //iamsopretty   public int getDistance() {
            //iamsopretty       return distance;
            //iamsopretty   }

        //iamsopretty   public int getTick() {
            //iamsopretty       return tick;
            //iamsopretty   }
        //iamsopretty    total tick 5,574
        //iamsopretty    s1 1,812
        //iamsopretty    s2 2469
        //iamsopretty    s3 1,293
    }
}
