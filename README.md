# 실행

## 맥/리눅스 실행

릴리즈의 tss폴더를 그대로 user.home에 드래그 합니다.
터미널에 가서 ->
nano ~/.zshrc
export PATH="$PATH:$HOME/tss"
Control + X
source ~/.zshrc
설정후
tssrun.sh 하면 게임이 실행됩니다.

## 윈도우

tss.jar를 실행하십시오.

## 실행시 필요한것

Java 21 JDK

